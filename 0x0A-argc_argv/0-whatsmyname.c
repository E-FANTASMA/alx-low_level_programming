#include "main.h"
#include <stdio.h>

/**
 * main - Prints its name
 *
 * @arc: line arguments
 *
 * @arv: array to be used
 *
 * Return: success
 */

int main(int arc, char *arv[])/*prototype to be included in main.h */
{
	printf("%s\n", arv[arc * 0]);

	return (0);
}
